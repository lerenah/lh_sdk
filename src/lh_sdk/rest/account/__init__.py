class Account:

  def __init__(self, lh_sdk, base_url, domain, version,):
    super(Account, self)
    self.lh_sdk = lh_sdk
    self.base_url = base_url
    self.domain = domain
    self.version = version
  
  